---
import type { EvidenceState } from "../../domain/evidence-types";
import { evidenceStateLabel } from "./presentation";

interface Props {
  state: EvidenceState;
}

const { state } = Astro.props;
---

<span class="evidence-badge" data-evidence-state={state}>{evidenceStateLabel(state)}</span>
